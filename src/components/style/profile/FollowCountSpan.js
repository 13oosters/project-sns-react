import styled from "styled-components";

const FollowCountSpan = styled.span`
  font-weight: 400;
  line-height: 1.2rem;
  font-size: ${(props) => props.theme.xSmallFontSize};
  color: ${(props) => props.theme.darkLightColor};
`;

export default FollowCountSpan;
